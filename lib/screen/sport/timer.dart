import 'package:flutter/material.dart';

class SportTimer extends StatelessWidget {
  const SportTimer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
